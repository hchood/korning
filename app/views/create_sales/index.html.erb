<h1>Listing create_sales</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @create_sales.each do |create_sale| %>
      <tr>
        <td><%= link_to 'Show', create_sale %></td>
        <td><%= link_to 'Edit', edit_create_sale_path(create_sale) %></td>
        <td><%= link_to 'Destroy', create_sale, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Create sale', new_create_sale_path %>
